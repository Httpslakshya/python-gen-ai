voice agent are the agents which take speech as input and return speech as output.


there are two way to create voice agent:

1. by using S2S model: these are the models that handle input & output directly by themselves and are for simple conversations ,and provide low latency
2. by using chained voice pipepine: these are best for predictable workflows or extending existing    text agent.